spring:
  kafka:
    bootstrap-servers: $D{KAFKA_SERVER:localhost:9092}
    producer:
      key-Serializer: org.apache.kafka.common.serialization.LongSerializer
      value-Serializer: org.springframework.kafka.support.serializer.JsonSerializer
